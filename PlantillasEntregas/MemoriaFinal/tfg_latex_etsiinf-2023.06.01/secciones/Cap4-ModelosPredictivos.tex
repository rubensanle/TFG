\chapter{Entrenamiento y validación de los modelos predictivos}
En este capítulo se describe la implementación, el entrenamiento y validación de los modelos predictivos seleccionados para este trabajo, dichos modelos son Random Forest, Gradient Boosting y LSTM. Para tal entrenamiento, se partirá de los datasets optimizados obtenidos en el Capítulo 3 (Se recuerda que había 2 datasets, uno de ellos para los modelos ML, con retardos en variables macroeconómicas y el otro para los modelos DL, sin retardos), se detalla la partición temporal de los datos, la configuración de hiperparámetros, el entrenamiento de los modelos y la evaluación de los mismos. En lo que respecta a los fundamentos teóricos de cada modelo, estos ya se presentaron en el Capítulo 2, por lo que aquí se abordará tan solo su aplicación práctica.

\section{División de los datos en entrenamiento, validación y prueba}

Una vez construidos los dos datasets, el siguiente paso es dividir los datos en tres subconjuntos, entrenamiento, validación y prueba. Dado que se trata de una serie temporal, la división debe de respetar el orden cronológico.

\subsection{Criterios de división}

Para dicha división, se ha optado por un reparto del 70\% de los datos para entrenamiento, 15\% para validación y 15\% para prueba. Esta distribución permite disponer de un número un amplio número de observaciones para el entrenamiento (212 meses), al igual que se tienen conjuntos de validación y prueba con tamaños significativos (26 meses cada uno). De este modo, se garantiza que los modelos tengan suficiente información para aprender patrones en los datos, al mismo tiempo que se dispone de conjuntos adecuados para ajustar hiperparámetros y evaluar el rendimiento final.

La división de los datos se ha implementado mediante una función en Python que respeta el orden secuencial de los datos, dicha función es \textit{division\_conjuntos}

\begin{lstlisting}[language=Python, caption=Función para la partición temporal de los datos, label=lst:split]
def division_conjuntos(dataframe, tr_size=0.7, vl_size=0.15, ts_size=0.15):
    N = dataframe.shape[0]
    Ntrain = int(tr_size * N)
    Nval = int(vl_size * N)
    
    train = dataframe.iloc[:Ntrain]
    val = dataframe.iloc[Ntrain:Ntrain+Nval]
    test = dataframe.iloc[Ntrain+Nval:]
    
    return train, val, test
\end{lstlisting}

\subsection{Aplicación a los datasets de ML y DL}

La función detallada anteriormente, se ha aplicado sobre ambos datasets, ya que tienen exactamente el mismo número de filas y las mismas fechas. De este modo, las divisiones temporales son idénticos para ambos conjuntos, lo que garantiza que la posterior comparación entre modelos sea justa y coherente.

\subsection{Funcionalidad de cada conjunto}

Como ya se ha comentado, la división se divide en tres conjuntos con funciones específicas durante el proceso de entrenamiento y evaluación de los modelos:

\begin{itemize}
    \item \textbf{Conjunto de entrenamiento}: Se utiliza para ajustar los parámetros internos de cada modelo. En el caso de Random Forest y Gradient Boosting, este conjunto se emplea para construir los árboles de decisión, en el caso de la LSTM, se usa para actualizar los pesos de la red mediante el algoritmo de retropropagación.
    
    \item \textbf{Conjunto de validación}: Se emplea durante el entrenamiento para ajustar los hiperparámetros, para decidir cuándo detener el entrenamiento y para evitar el sobreajuste. 
    
    \item \textbf{Conjunto de prueba}: Este conjunto, se usa únicamente para la evaluación final del modelo. Una vez que el modelo ha sido entrenado y ajustado, se calculan las métricas de error sobre este conjunto para obtener una estimación de su capacidad de generalización. 
\end{itemize}

\section{Entrenamiento de modelos de Machine Learning}
Una vez divididos los datos en los conjuntos de entrenamiento, validación y prueba, se procede al entrenamiento de los dos modelos de Machine Learning. Ambos modelos se entrenan sobre el dataset_ml (Dataset con retardos para los modelos de Machine Learning). Sin embargo, también se probará a entrenarlos con el dataset sin selección de variables, para asi ver todas las posibilidades.

Para cada modelo se han seguido dos etapas. En primer lugar, se ha realizado una búsqueda de hiperparámetros con el objetivo de encontrar la configuración que minimice el error en el conjunto de validación. En segundo lugar, una vez seleccionados los mejores hiperparámetros, se ha entrenado el modelo final sobre el conjunto de entrenamiento completo. 

\subsection{Random Forest}

A continuación, se describe el proceso de ajuste de hiperparámetros y entrenamiento del modelo Random Forest. Pero antes de continuar, es importante comentar que se han obtenido mejores resultados al trabajar con el conjunto completo de variables que con el conjunto reducido. Es por ello, que los resultados mostrados a continuación son los obtenidos tras el entrenamiento con el dataset completo. 

\subsubsection{Configuración de hiperparámetros}

Para el modelo Random Forest se ha definido un espacio de búsqueda que incluye los siguientes hiperparámetros:

\begin{itemize}
    \item \textit{n\_estimators}: número de árboles del bosque (100, 150, 200, 250, 300).
    \item \textit{max\_depth}: profundidad máxima de cada árbol (5, 7, 10, 12, 15, 20, None).
    \item \textit{min\_samples\_split}: número mínimo de muestras requerido para dividir un nodo (2, 5, 7, 10).
    \item \textit{min\_samples\_leaf}: número mínimo de muestras requerido en una hoja (1, 2, 3, 4).
\end{itemize}

El ajuste de hiperparámetros se ha realizado mediante búsqueda en rejilla (\texttt{GridSearchCV}) \cite{scikit_grid} combinada con validación cruzada temporal (\texttt{TimeSeriesSplit}) \cite{scikit_ts} con 3 divisiones, garantizando asi el orden cronológico de los datos. En cuanto a La métrica de optimización, esta ha sido el error cuadrático medio (\textit{MSE}) \cite{mse}.

Tras la busqueda de hiperparámetros, los mejores obtenidos han sido:

\begin{itemize}
    \item \textit{n\_estimators} = 100
    \item \textit{max\_depth} = 15
    \item \textit{min\_samples\_split} = 4
    \item \textit{min\_samples\_leaf} = 2
\end{itemize}

El mejor MAE alcanzado durante el entrenamiento ha sido de 0,0701. Realizando la misma busqueda pero con el dataset reducido, el mejor MAE obtenido ha sido de 0,0095, lo que confirma que el modelo obtiene mejores resultados al trabajar con el conjunto completo de variables.

\subsubsection{Entrenamiento}

Una vez seleccionados los mejores hiperparámetros, se ha entrenado el modelo Random Forest final sobre el conjunto de entrenamiento completo (212 meses). Para ello, se ha utilizado la implementación \textit{RandomForestRegressor} de la librería scikit-learn \cite{scikit_rf}. Tras el entrenamiento, el modelo ha sido guardado para su posterior uso en la fase de evaluación.

En cuanto a las métricas de error obtenidas en la fase de validación, se ha obtenido lo siguiente:

\begin{itemize}
    \item \textbf{MAE} (Error Absoluto Medio): 0,0766
    \item \textbf{RMSE} (Raíz del Error Cuadrático Medio): 0,0946
    \item \textbf{R2} (Coeficiente de Determinación): -0,3221
\end{itemize}

Estos valores indican que, de media, la predicción del modelo se desvía aproximadamente un 7,66\% del rendimiento real de Apple, un resultado razonable teniendo en cuenta la alta volatilidad de los mercados financieros. Puntualizar que estos valores, también son menores que los obtenidos con el dataset reducido.

\subsection{Gradient Boosting}

En lo que respecta al modelo Gradient Boosting, se ha seguido la misma metodología que en el caso anterior, búsqueda de hiperparámetros con validación cruzada temporal y entrenamiento final con el conjunto completo de variables.

\subsubsection{Configuración de hiperparámetros}

Para el modelo Gradient Boosting se ha definido el siguiente espacio de búsqueda:

\begin{itemize}
    \item \textit{n\_estimators}: número de árboles (100, 150, 200, 250, 300).
    \item \textit{learning\_rate}: tasa de aprendizaje (0.005, 0.01, 0.03, 0.05, 0.1).
    \item \textit{max\_depth}: profundidad máxima de cada árbol (3, 5, 7, 9).
    \item \textit{subsample}: fracción de muestras utilizada en cada iteración (0.4, 0.6, 0.8, 1.0).
    \item \textit{min\_samples\_split}: mínimo de muestras para dividir un nodo (2, 5, 8, 10, 12, 15).
\end{itemize}

Al igual que en el caso de Random Forest, el ajuste se ha realizado con \texttt{GridSearchCV} y validación cruzada temporal \texttt{TimeSeriesSplit} con 3 divisiones y los mejores hiperparámetros obtenidos han sido:

\begin{itemize}
    \item \texttt{n\_estimators} = 100
    \item \texttt{learning\_rate} = 0,005
    \item \texttt{max\_depth} = 9
    \item \texttt{subsample} = 0,4
    \item \texttt{min\_samples\_split} = 8
\end{itemize}

El mejor MAE alcanzado durante el entrenamiento ha sido de 0,0664.

\subsubsection{Entrenamiento}

Con los mejores hiperparámetros seleccionados, se ha entrenado el modelo Gradient Boosting final sobre el conjunto de entrenamiento. Para ello, se ha utilizado la implementación \textit{GradientBoostingRegressor} de scikit-learn \cite{scikit_gb}.

Tras el entrenamiento, las métricas de error obtenidas sobre el conjunto de validación han sido:

\begin{itemize}
    \item \textbf{MAE} (Error Absoluto Medio): 0,0727
    \item \textbf{RMSE} (Raíz del Error Cuadrático Medio): 0,0847
\end{itemize}

Estos resultados son ligeramente mejores que los obtenidos con Random Forest, lo que indica que, para este problema, el enfoque de boosting ha resultado más efectivo que el de bagging.

\section{Modelo de Deep Learning: LSTM}

Pasando ahora a describir la implementación de la red LSTM, un tipo de red neuronal recurrente diseñada para capturar dependencias temporales en datos secuenciales. Este modelo LSTM, a diferencia de los modelos de Machine Learning, requiere un preprocesamiento adicional y una arquitectura específica para poder aprovechar su capacidad de memoria a largo plazo. Por otro lado, en el caso de Deep Learning, se ha entrenado el modelo tanto con el dataset completo de variables como con el dataset reducido, obteniendo mejores resultados con el dataset reducido, por lo que se mostrarán los resultados obtenidos con dicho dataset.

\subsection{Preprocesamiento específico para LSTM}
Los procesos de preprocesamiento para la red LSTM, son primero de todo un escalado de los datos \cite{escalado_datos} y posteriormente la creación de secuencias temporales. A continuación, se detallan ambos procesos:

\subsubsection{Escalado de los datos}

A diferencia de los modelos Random Forest y Gradient Boosting, que son modelos que no varian ante transformaciones lineales de las variables, las redes neuronales son sensibles a la escala de los datos de entrada. Es por ello que, es necesario escalar todas las variables para que se encuentren en un rango similar, ayudand asi al alhoritmo durante el entrenamiento. El proces de escalado en este trabajo se ha llevado a cabo a través de la técnica \texttt{MinMaxScaler} de scikit-learn \cite{minmaxscaler}, que transforma los datos al rango $[-1, 1]$. 

\subsubsection{Creación de secuencias temporales}

El modelo LSTM no procesa las observaciones de forma independiente, sino que necesita una serie de pasos para predecir el siguiente valor. Por esto mismo, se ha definido una ventana de entrada de \texttt{INPUT\_LENGTH = 6} meses, es decir, el modelo utiliza los seis meses anteriores para realizar la predicción. Por otro lado, el número de meses a predecir es de \texttt{OUTPUT\_LENGTH = 6} meses, lo que permite evaluar la capacidad del modelo para predecir a medio plazo. Por último, se hadefinido en Python una función llamada \texttt{crear\_secuencias}, que transforma el dataset original en un conjunto de secuencias supervisadas, donde cada muestra de entrada es un array de dimensiones \texttt{(INPUT\_LENGTH, num\_features)} y la salida es un array de dimensiones \texttt{(OUTPUT\_LENGTH, 1)}.

\subsection{Arquitectura de la red}

Pasando ahora a la arquitectura de la red, esta se ha diseñado de forma sencilla para asi poder adaptarse al tamaño reducido del dataset. Para ello, se ha utilizado una única capa LSTM con 32 neuronas. A continuación, se ha añadido una capa de salida densa con activación lineal y con 6 neuronas (meses a predecir). A su vez, se ha incluido una tasa de \texttt{dropout} \cite{dropout} del 20\% tanto en la entrada como en las conexiones recurrentes para asi poder evitar el sobreajuste. 

\subsection{Compilación y entrenamiento}

El modelo se ha compilado utilizando \texttt{Adam} \cite{Adam}, se le ha establecido una tasa de aprendizaje de 0,001. En cuanto a la función de perdida empleada, esta ha sido error cuadrático medio (MSE), también se ha seguido el error absoluto medio (MAE). Para acabar con la compilación del modelo, durante la misma, se han utilizado dos \textit{callbacks} \cite{callbacks}, uno de ellos ha sido \texttt{EarlyStopping} \cite{earlystopping} con paciencia de 10 épocas para detener el entrenamiento cuando la pérdida en validación dejase de mejorar y restaurando los pesos del mejor modelo, también se ha usado \texttt{ReduceLROnPlateau} \cite{reduce_lr_on_plateau} para reducir la tasa de aprendizaje a la mitad si la pérdida en validación no mejoraba durante 5 épocas, con un mínimo de $10^{-6}$.

En cuanto al entrenamiento, este se ha realizado durante un máximo de 100 épocas con un (\textit{batch size}) de 32 muestras. Durante dicho entrenamiento, el proceso se detuvo en la época 54, y se restauraron los pesos de la época 44, época en la que se obtuvo el menor error de validación.

En la Figura \ref{fig:lstm_training} se muestran las curvas de evolución de la pérdida (MSE) y del error absoluto medio (MAE) durante el entrenamiento. Se observa que tanto la pérdida como el MAE disminuyen progresivamente, y que la diferencia entre las curvas de entrenamiento y validación es reducida, lo que indica que el modelo no presenta problemas de sobreajuste.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{imagenes/lstm_training_curves.png}
\caption{Evolución de la pérdida (MSE) y del MAE durante el entrenamiento de la LSTM.}
\label{fig:lstm_training}
\end{figure}

Los mejores resultados obtenidos en el conjunto de validación fueron un MSE de 0,0818 y un MAE de 0,2375. Estos valores, son algo más altos que los obtenidos en los modelos Random Forest y Gradient Boosting, Sin embargo, los valores obetenidos demuestran que la LSTM es capaz de aprender patrones temporales pese a estar su rendimiento limitado por el reducido tamaño del conjunto de datos y la alta volatilidad inherente a las series financieras.

\section{Evaluación de los modelos}
En esta sección se presentan los resultados obtenidos por cada modelo en el conjunto de prueba, utilizando las métricas de error, al igual que en el backtesting. Además, se realiza una comparativa global entre los tres modelos para determinar cuál ha sido el más efectivo en la predicción del rendimiento de Apple.

\subsection{Métricas de evaluación}
Antes de nada, mencionar que las metricas de evaluación utilizadas para comparar los modelos han sido las siguientes:
\begin{itemize}
    \item \textbf{MAE} (Error Absoluto Medio): Mide el error promedio absoluto entre las predicciones y los valores reales. Es una métrica fácil de interpretar, ya que se expresa en las mismas unidades que la variable objetivo.
    
    \item \textbf{RMSE} (Raíz del Error Cuadrático Medio): Mide la raíz cuadrada del promedio de los errores al cuadrado. Penaliza más los errores grandes que el MAE, lo que puede ser útil para detectar modelos que cometen errores significativos.
    
    \item \textbf{R2} (Coeficiente de Determinación): Mide la proporción de la varianza en la variable objetivo que es explicada por el modelo. Un valor de R2 cercano a 1 indica un buen ajuste del modelo a los datos, mientras que un valor cercano a 0 indica un ajuste pobre.
\end{itemize}

\subsection{Estrategia de backtesting}
Para evaluar la robustez temporal de los modelos y evitar conclusiones optimistas derivadas de una única partición, se ha llevado a cabo un \textbf{backtesting} con ventana deslizante. La estrategia consiste en entrenar el modelo con una ventana histórica de datos y evaluarlo sobre el período siguiente, desplazando la ventana secuencialmente. En concreto, se ha utilizado una ventana de entrenamiento de 200 meses (16,7 años) y una ventana de prueba de 12 meses (1 año), con un desplazamiento de 12 meses entre iteraciones. Este enfoque simula el comportamiento real del modelo en diferentes períodos históricos, incluyendo la crisis financiera de 2008, la pandemia de COVID-19 y el posterior episodio inflacionario.

\subsection{Resultados de Random Forest}
A continuación, se presentan los resultados obtenidos por el modelo Random Forest en el conjunto de prueba y en el backtesting.

\subsubsection{Evaluación en el conjunto de prueba}
Sobre el conjunto de prueba, el modelo Random Forest obtuvo las siguientes métricas:

\begin{itemize}
    \item \textbf{MAE}: 0,0453 (4,53\% de error medio) Este valor indica que, en promedio, las predicciones del modelo se desvían un 4,53\% del rendimiento real de Apple, lo que representa una mejora significativa respecto a los resultados obtenidos en la fase de validación.
    \item \textbf{RMSE}: 0,0612 (6,12\% de error cuadrático medio) Este valor, aunque algo más alto que el MAE, sigue indicando un buen rendimiento del modelo, ya que penaliza más los errores grandes. La diferencia entre el MAE y el RMSE sugiere que el modelo comete algunos errores más significativos, pero en general mantiene una buena precisión.
    \item \textbf{R²}: 0,5033 (50,33\% de la varianza explicada) Este valor indica que el modelo es capaz de explicar aproximadamente el 50\% de la variabilidad del rendimiento mensual de Apple, lo que se considera un resultado sólido para un problema de predicción financiera, dada la complejidad y volatilidad inherente a los mercados.
\end{itemize}

Atendiendo a los valores obtenidos, se puede concluir que el modelo Random Forest ha logrado un rendimiento bastante bueno en la predicción del rendimiento de Apple, superando con creces los resultados obtenidos durante la fase de validación. Esto sugiere que el modelo ha logrado generalizar adecuadamente a datos no vistos durante el entrenamiento.

En la Figura \ref{fig:rf_test_predictions} se muestra un gráfico comparativo entre las predicciones del modelo y los valores reales del rendimiento de Apple en el conjunto de prueba. Se observa que las predicciones siguen de cerca la tendencia general del rendimiento real, aunque existen algunas desviaciones puntuales, especialmente en meses con movimientos más bruscos.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{imagenes/rf_test_predictions.png}
\caption{Comparativa entre las predicciones del modelo Random Forest y los valores reales del rendimiento de Apple en el conjunto de prueba.}
\label{fig:rf_test_predictions}
\end{figure}    

\subsubsection{Backtesting}

En cuanto al backtesting, este produjo los siguientes resultados:

\begin{itemize}
    \item \textbf{MAE medio}: 0,0698 (±0,0122) Este valor medio de MAE a lo largo de las diferentes ventanas de backtesting indica que, en promedio, el modelo se desvía un 6,98\% del rendimiento real de Apple. La desviación estándar de ±0,0122 sugiere que el rendimiento del modelo es relativamente consistente a lo largo del tiempo, aunque existen algunas variaciones dependiendo del período evaluado.
    \item \textbf{RMSE medio}: 0,0843 (±0,0147) Este valor medio de RMSE indica que, en promedio, el modelo comete errores cuadráticos medios del 8,43\%.
    \item \textbf{R² medio}: -0,2835 (±0,1438) Este valor medio de R² negativo indica que, en promedio a lo largo de las diferentes ventanas, el modelo no consigue superar a un predictor ingenuo basado en la media histórica. Sin embargo, es importante señalar que el rendimiento del modelo es muy variable dependiendo del período: en épocas de mercado tendencial (como la recuperación post-COVID) el modelo funciona bien, mientras que en períodos de alta volatilidad o cambios de régimen (como la crisis de 2008) su rendimiento se deteriora significativamente. Esta limitación es inherente a la naturaleza no estacionaria de los mercados financieros y al reducido tamaño del conjunto de entrenamiento (200 meses).
\end{itemize}

Con los calores aanteriores, se puede concluir que el modelo Random Forest muestra un rendimiento sólido en la predicción del rendimiento de Apple, aunque su capacidad para generalizar a diferentes períodos históricos es limitada, lo que refleja la complejidad y volatilidad de los mercados financieros. Dicha limitación viene dada también por el reducido tamaño del conjunto de entrenamiento.

\subsection{Resultados de Gradient Boosting}
A continuación, se presentan los resultados obtenidos por el modelo Gradient Boosting en el conjunto de prueba y en el backtesting.

\subsubsection{Evaluación en el conjunto de prueba}
El modelo Gradient Boosting obtuvo los siguientes resultados sobre el conjunto de prueba:

\begin{itemize}
    \item \textbf{MAE}: 0,0613 (6,13\% de error medio) Este valor indica que en promedio, las predicciones del modelo se desvían un 6,13\% del rendimiento real de Apple, lo que representa una mejora respecto a los resultados obtenidos durante la fase de validación, aunque es algo inferior al rendimiento alcanzado por el modelo Random Forest.
    \item \textbf{RMSE}: 0,0774 (7,74\% de error cuadrático medio) Este valor, aunque algo más alto que el MAE, sigue indicando un rendimiento razonable del modelo, aunque es ligeramente inferior al obtenido por Random Forest, lo que sugiere que el modelo comete algunos errores más significativos.
    \item \textbf{R²}: 0,2049 (20,49\% de la varianza explicada) Este valor indica que el modelo es capaz de explicar aproximadamente el 20\% de la variabilidad del rendimiento mensual de Apple, lo que se considera un resultado modesto para un problema de predicción financiera, aunque sigue siendo una mejora respecto a los resultados obtenidos durante la fase de validación.
\end{itemize}

Según los valores obtenidos, se puede concluir que el modelo Gradient Boosting ha logrado un rendimiento razonable en la predicción del rendimiento de Apple, aunque su capacidad de generalización es inferior a la del modelo Random Forest. Esto sugiere que, para este problema específico, el enfoque de boosting no ha sido tan efectivo como el de bagging.

En la Figura \ref{fig:gb_test_predictions} se muestra un gráfico comparativo entre las predicciones del modelo Gradient Boosting y los valores reales del rendimiento de Apple en el conjunto de prueba. Se observa que las predicciones siguen la tendencia general del rendimiento real, aunque existen desviaciones más pronunciadas que en el caso de Random Forest, especialmente en meses con movimientos más bruscos.
\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{imagenes/gb_test_predictions.png}
\caption{Comparativa entre las predicciones del modelo Gradient Boosting y los valores reales del rendimiento de Apple en el conjunto de prueba.}
\label{fig:gb_test_predictions}
\end{figure}

\subsubsection{Backtesting}
En cuanto a los valores obtenidos en el backtesting, estos han sido los siguientes:

\begin{itemize}
    \item \textbf{MAE medio}: 0,0655 (±0,0108) Este valor medio de MAE a lo largo de las diferentes ventanas de backtesting indica que, en promedio, el modelo se desvía un 6,55\% del rendimiento real de Apple. La desviación estándar de ±0,0108 sugiere que el rendimiento del modelo es relativamente consistente a lo largo del tiempo, aunque existen algunas variaciones dependiendo del período evaluado.
    \item \textbf{RMSE medio}: 0,0778 (±0,0106) Este valor medio de RMSE indica que, en promedio, el modelo comete errores cuadráticos medios del 7,78\%, lo que es ligeramente superior al obtenido por Random Forest, aunque la desviación estándar es menor, lo que sugiere un rendimiento más consistente a lo largo del tiempo.
    \item \textbf{R² medio}: -0,0963 (±0,0383) Este valor medio de R² negativo indica que, en promedio a lo largo de las diferentes ventanas, el modelo no consigue superar a un predictor ingenuo basado en la media histórica. Sin embargo, es importante señalar que el rendimiento del modelo es más consistente a lo largo del tiempo que el de Random Forest, con menos variabilidad entre períodos, aunque sigue mostrando dificultades para capturar los cambios de régimen y la alta volatilidad de los mercados financieros.
\end{itemize}

Analizando los valores anteriores, se puede concluir que el modelo Gradient Boosting muestra un rendimiento razonable en la predicción del rendimiento de Apple, aunque su capacidad para generalizar a diferentes períodos históricos es limitada, lo que refleja la complejidad y volatilidad de los mercados financieros. Sin embargo, su rendimiento es algo más consistente a lo largo del tiempo que el de Random Forest, aunque sigue mostrando dificultades para capturar los cambios de régimen y la alta volatilidad de los mercados financieros.

\subsection{Resultados de LSTM}

\subsubsection{Evaluación en el conjunto de prueba}

\subsubsection{Backtesting}

\subsection{Comparativa global}

\section{Conclusiones del capítulo}
